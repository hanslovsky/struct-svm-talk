% References with biber. 
\usepackage[
    backend = biber,
    firstinits = true,
    % style = phys, % the phys style must be installed separately
    sortlocale = en_US,
    natbib = true,
    doi = false,
    eprint = false,
    sorting=nyt,
    style=authoryear,
    maxbibnames=99,
    maxcitenames=2,
    uniquelist=false
]{biblatex}


%%% Local Variables: 
%%% mode: latex
%%% TeX-master: "../../main"
%%% End: 
